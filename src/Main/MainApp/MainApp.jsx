import { BrowserRouter as Router } from "react-router-dom";

import MainRouter from "./../MainRoute/MainRouter";

function MainApp() {
  return (
    <Router>
      <MainRouter />
    </Router>
  );
}

export default MainApp;
